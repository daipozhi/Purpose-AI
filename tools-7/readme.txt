before step50,
run program ./a-step50-ini.exe 200 8
    ('200' is total file number, '8' is how many CPU thread do you want use)
will generate 'a-step50.bat'

在 step50 之前运行 ./a-step50-ini.exe 200 8
'200' 是要计算的文件的数量,'8'是使用的 CPU 线程数量 .
将生成批命令 'a-step50.bat'

before step90,
run program ./a-step90-ini.exe 200 8
    ('200' is total file number, '8' is how many CPU thread do you want use)
will generate 'a-step90.bat'

在 step90 之前运行 ./a-step90-ini.exe 200 8
'200' 是要计算的文件的数量,'8'是使用的 CPU 线程数量 .
将生成批命令 'a-step90.bat'

before step210,
run program ./a-step210-ini.exe 200 8
    ('200' is total file number, '8' is how many CPU thread do you want use)
will generate 'a-step210.bat'

在 step210 之前运行 ./a-step210-ini.exe 200 8
'200' 是要计算的文件的数量,'8'是使用的 CPU 线程数量 .
将生成批命令 'a-step210.bat'

how to show prograss:
input 
$./moniter
(ps -ef|grep a-step)(in tools-6 directory)

怎样显示进度，输入
$./moniter
(ps -ef|grep a-step)(在 tools-7 目录)
