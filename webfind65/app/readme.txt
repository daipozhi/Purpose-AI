this is app directory,use app-separ.exe to separate sentence,
usage: './app-separ.exe myfile.txt' , the result is in file myfile.out2.txt
below files are copied from work_dir
  words04.txt
  grammar-base04.sort.txt
  grammar-cw03.sort.txt
  ...
in myfile.out2.txt, lines start with '$1,,' are first step's result,
lines start with '$3,,' are final result.


这是一个app目录，用 app_separ.exe 去分解句子，用法：
 ./app_separ.exe myfile.txt ，结果在myfile.out2.txt文件里，
以下文件是从 work_dir 拷贝来的 。
  words04.txt
  grammar-base04.sort.txt
  grammar-cw03.sort.txt
  ...
在myfile.out2.txt里, 用"$1,,"开始的行是第一步结果,
用"$3,,"开始的行是第二步结果,第二步结果是 最后结果.



